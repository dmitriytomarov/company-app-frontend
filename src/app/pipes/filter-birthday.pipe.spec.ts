import { FilterBirthdayPipe } from './filter-birthday.pipe';

describe('FilterBirthdayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBirthdayPipe();
    expect(pipe).toBeTruthy();
  });
});

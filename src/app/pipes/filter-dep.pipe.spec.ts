import { FilterDepPipe } from './filter-dep.pipe';

describe('FilterDepPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDepPipe();
    expect(pipe).toBeTruthy();
  });
});
